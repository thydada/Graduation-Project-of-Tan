# GPOT 快递管理系统 - 功能模块图（竖向）

以下为竖向排布的功能模块图，可在支持 Mermaid 的编辑器中预览，或复制到 [Mermaid Live Editor](https://mermaid.live) 导出为图片。

```mermaid
flowchart TB
    subgraph system["GPOT 快递管理系统"]
        direction TB

        subgraph M1["认证与登录模块"]
            M1a["多角色登录(管理员/员工/普通用户)"]
            M1b["登录验证与授权"]
            M1c["登录日志记录"]
        end

        subgraph M2["用户与角色管理模块"]
            subgraph M2a["管理员管理"]
                M2a1["管理员账号维护"]
                M2a2["管理员权限配置"]
            end
            subgraph M2b["员工管理"]
                M2b1["员工基本信息维护"]
                M2b2["员工部门与职位维护"]
            end
            subgraph M2c["普通用户管理"]
                M2c1["用户注册信息维护"]
                M2c2["身份证信息管理"]
            end
            subgraph M2d["角色与权限"]
                M2d1["角色定义"]
                M2d2["权限分配"]
            end
        end

        subgraph M3["仓库管理模块"]
            subgraph M3a["仓库信息管理"]
                M3a1["仓库基础信息"]
                M3a2["仓库启停状态"]
            end
            subgraph M3b["货架与库位管理"]
                M3b1["货架信息"]
                M3b2["库位配置"]
                M3b3["货架与仓库关联"]
            end
        end

        subgraph M4["包裹管理与追踪模块"]
            subgraph M4a["包裹基础信息"]
                M4a1["包裹登记"]
                M4a2["包裹与寄件人/收件人关联"]
            end
            subgraph M4b["包裹在库管理"]
                M4b1["入库登记"]
                M4b2["出库登记"]
                M4b3["库存状态维护"]
            end
        end

        subgraph M5["前端界面模块"]
            M5a["登录界面 (Vue 3)"]
            M5b["管理后台界面(用户/仓库/包裹)"]
            M5c["响应式布局与交互"]
        end

        subgraph M6["统计分析与报表模块"]
            subgraph M6a["业务数据统计"]
                M6a1["包裹数量统计"]
                M6a2["仓库吞吐量统计"]
                M6a3["用户规模统计"]
            end
        end
    end

    M1 ~~~ M2
    M2 ~~~ M3
    M3 ~~~ M4
    M4 ~~~ M5
    M5 ~~~ M6
```

## 模块说明（自上而下）

1. **认证与登录模块**：多角色登录、登录验证与授权、登录日志记录  
2. **用户与角色管理模块**：管理员管理、员工管理、普通用户管理、角色与权限  
3. **仓库管理模块**：仓库信息管理、货架与库位管理  
4. **包裹管理与追踪模块**：包裹基础信息、包裹在库管理  
5. **前端界面模块**：登录界面、管理后台界面、响应式布局与交互  
6. **统计分析与报表模块**：业务数据统计（包裹数量、仓库吞吐量、用户规模）
