# 4.2 后端核心功能实现

## 4.2.1 用户认证模块

认证功能的实现位于 `AuthController` 和 `AuthService` 类中。系统支持三种角色的登录验证：管理员、员工和普通用户。登录接口接收 `userType`、`username`、`password` 三个参数，根据 `userType` 调用对应 Repository 的查询方法验证用户身份。如果验证成功，返回包含用户信息的响应对象，包括用户ID、真实姓名、用户类型等基本信息。

用户注册功能仅面向普通用户开放。注册时需要提供用户名、密码和真实姓名。系统会先检查用户名是否已存在，避免重复注册。

## 4.2.2 包裹管理模块

包裹管理功能是系统最核心的模块，涉及包裹的完整生命周期管理。

包裹创建功能通过 Debug 接口实现，允许直接向包裹表中写入包裹记录，包裹初始状态为"待入库"。

包裹入库功能由 `inboundFormalPackage` 方法实现。当员工对"待入库"状态的包裹执行入库操作时，系统会验证包裹状态，分配货架位置和层数，生成取件码，更新包裹状态为"已入库"，并创建入库记录。入库完成后，系统会自动向用户发送取件提醒消息。

包裹出库功能由 `outboundPackage` 方法实现。员工将"已入库"的包裹状态更新为"运输中"，并创建出库记录。系统会随机分配派送员工。

用户取件功能通过终端机扫码实现。系统根据快递单号查询包裹，验证包裹状态为"已入库"或"待取件"，更新状态为"已取件"，并删除 Redis 中的超时检测键。

包裹查询功能支持多种查询方式：按用户ID查询、按快递单号查询、按收件人电话查询、获取用户全部包裹（含分页和关键词搜索）。

## 4.2.3 货架分配与取件码生成

货架自动分配算法根据包裹的尺寸和重量自动选择合适的货架位置。系统解析包裹的尺寸信息计算体积，如果体积大于50000立方厘米或重量大于5公斤，则判定为大件，分配到大货架（货架4）；否则使用普通货架（货架1-3），系统会选择当前使用量最少的货架。在选定的货架上，选择当前存放数量最少的层。

取件码生成算法采用 `SecureRandom` 生成安全的随机数。取件码格式为"货架ID-层数-6位随机字符"，随机字符从预先定义的字符集（排除0、O、1、I、L等易混淆字符）中选取。生成后系统会检查取件码是否已存在，确保唯一性。

## 4.2.4 异常件管理

异常件登记功能将正式包裹标记为异常状态，并在异常件表中创建记录。记录内容包括异常类型、异常原因、报告人信息等。同时将原包裹的状态更新为"异常"。

取件超时自动处理功能通过 Redis 键过期监听实现。当包裹入库时，系统在 Redis 中设置超时键。超时时间到达后，Redis 触发过期事件，监听器自动将超时未取件的包裹标记为异常，无需人工干预。

异常件查询功能支持获取所有异常件列表，按报告时间倒序排列。

## 4.2.5 消息通知模块

消息通知功能在 `MessageController` 和 `MessageService` 类中实现。系统具备完整的消息管理功能，包括查询用户消息列表、查询未读消息、统计未读数量、标记单条消息已读、标记所有消息已读、群发消息等。

取件提醒消息在包裹入库时自动发送。消息内容包含快递单号、取件码、货架位置等信息，方便用户便捷取件。

群发消息功能允许管理员或员工向所有用户发送系统公告等通知消息。

## 4.2.6 管理员统计模块

系统统计功能提供全面的运营数据。`getAdminStatistics` 方法返回系统的各类统计数据，包括：各状态包裹数量（待入库、已入库、运输中、已取件、异常）、异常件处理状况（待处理、处理中、已处理）、用户和员工数量、操作记录数量等。

每日入库趋势统计功能通过 `getDailyEntryStatistics` 方法实现，返回近N日的入库统计数据，按日期分组统计入库记录数量，用于绘制趋势图表。

运维数据导出功能将统计数据整理成文本文件，保存到服务器的项目目录下，便于运维人员存档和分析。

## 4.2.7 AI 分析服务集成

AI 分析服务的实现位于 `AiService` 类中，系统集成了 SiliconFlow 平台的 API 接口，使用 Qwen/Qwen2.5-14B-Instruct 模型进行数据分析。

AI 服务调用流程包括：接收待分析的数据内容，构建符合 API 要求的请求参数（包含模型名称、消息内容等），设置 HTTP 请求头（Content-Type 和 Authorization），发送 POST 请求到 SiliconFlow API 端点，解析返回的 JSON 响应并提取分析结果。

系统支持自定义提示词功能，开发人员可在配置文件中设置 `ai.prompt` 参数定制分析风格。若未配置提示词，系统使用默认的通用提示词进行分析。

## 4.2.8 Redis 超时监听模块

Redis 超时监听功能通过 `RedisKeyExpirationListener` 类实现。该监听器继承自 Spring Data Redis 的 `KeyExpirationEventMessageListener`，用于监听 Redis 中包裹超时键的过期事件。

当包裹入库时，系统会在 Redis 中写入带过期时间的键。当超时时间到达后，Redis 自动删除该键并发布过期事件。监听器捕获事件后，从键名中提取包裹ID，调用 `PackageService` 的 `handlePickupTimeout` 方法处理超时逻辑，自动将超时包裹标记为异常状态。
