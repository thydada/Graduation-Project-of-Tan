# GPOT 快递管理系统 - 项目详细说明文档

## 一、项目概述

GPOT（快递运营管理系统）是一套基于 SpringBoot + MySQL + Vue.js 开发的快递驿站综合管理平台。该系统主要用于快递驿站的日常运营管理，包括包裹的入库、出库、运输、异常处理、消息通知等核心业务流程，同时为管理员提供运营数据统计与分析功能。

### 1.1 项目技术栈

| 层次 | 技术选型 | 版本 |
|------|----------|------|
| 后端框架 | Spring Boot | 4.0.1 |
| 数据持久层 | Spring Data JPA | - |
| 数据库 | MySQL | - |
| 缓存 | Redis | - |
| 前端框架 | Vue.js | 3.x |
| 前端构建工具 | Vite | - |
| UI路由 | Vue Router | - |
| HTTP客户端 | Axios | - |

### 1.2 项目目录结构

```
Graduation Project of Tan/
├── GPOT/                          # 后端Spring Boot项目
│   ├── src/main/java/com/example/gpot/
│   │   ├── GpotApplication.java   # 启动类
│   │   ├── controller/             # 控制器层（3个）
│   │   │   ├── AuthController.java
│   │   │   ├── PackageController.java
│   │   │   └── MessageController.java
│   │   ├── service/                # 服务层（4个）
│   │   │   ├── AuthService.java
│   │   │   ├── PackageService.java
│   │   │   ├── MessageService.java
│   │   │   └── AiService.java
│   │   ├── repository/             # 数据访问层（9个）
│   │   ├── entity/                 # 实体类（8个）
│   │   └── dto/                    # 数据传输对象（7个）
│   ├── src/main/resources/
│   │   └── application.properties  # 配置文件
│   ├── pom.xml                     # Maven依赖配置
│   └── START-GUIDE.md              # 启动指南
│
├── GPOT-F/                         # 前端Vue项目
│   ├── src/
│   │   ├── main.js                 # 入口文件
│   │   ├── App.vue                 # 根组件
│   │   ├── router/
│   │   │   └── index.js            # 路由配置
│   │   ├── services/
│   │   │   └── api.js              # API服务
│   │   └── views/                  # 视图组件（20个）
│   │       ├── LoginView.vue       # 登录页面
│   │       ├── AdminDashboardView.vue    # 管理员仪表盘
│   │       ├── EmployeeLayout.vue        # 员工布局
│   │       ├── UserPackagesView.vue      # 用户包裹
│   │       ├── TerminalQueryView.vue     # 终端查询
│   │       └── ...其他视图
│   ├── vite.config.js              # Vite配置
│   └── package.json                # NPM依赖
│
├── 需求.md                         # 原始需求文档
├── 需求实现程度说明.md              # 实现程度说明
└── 面试题与答案.md                 # 面试题汇总
```

---

## 二、系统架构设计

### 2.1 整体架构

GPOT系统采用经典的三层架构设计：

```
┌─────────────────────────────────────────────────────────────┐
│                      前端层 (Vue.js)                        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │
│  │ 管理员  │ │  员工   │ │  用户   │ │   终端查询机    │   │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────────┬────────┘   │
└───────┼───────────┼───────────┼───────────────┼────────────┘
        │           │           │               │
        └───────────┴───────────┼───────────────┘
                                │
                    ┌───────────▼───────────┐
                    │    API网关层 (REST)    │
                    │    /api/login          │
                    │    /api/packages      │
                    │    /api/messages      │
                    │    /api/admin        │
                    └───────────┬───────────┘
                                │
┌───────────────────────────────┼───────────────────────────────┐
│                    业务逻辑层 (Spring Boot)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐     │
│  │ AuthService │  │PackageService│  │ MessageService  │     │
│  │ (认证服务)  │  │ (包裹服务)   │  │   (消息服务)    │     │
│  └──────┬──────┘  └──────┬──────┘  └────────┬────────┘     │
│         │                 │                   │              │
│  ┌──────┴─────────────────┴───────────────────┴──────────┐ │
│  │                    AiService (AI分析服务)               │ │
│  └──────────────────────────┬──────────────────────────────┘ │
└─────────────────────────────┼───────────────────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────┐
│                    数据持久层 (JPA + MySQL)                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ User表   │ │Employee表│ │ Admin表  │ │Package表 │      │
│  │Message表 │ │Exception表│ │Shelf表  │ │Outbound表│      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 数据库设计

系统包含以下核心数据表：

#### 2.2.1 用户相关表

**user 表（普通用户）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(255) | 密码 |
| realName | VARCHAR(50) | 真实姓名 |
| phone | VARCHAR(20) | 电话号码 |
| email | VARCHAR(100) | 邮箱 |
| idCard | VARCHAR(18) | 身份证号 |
| status | TINYINT | 状态（1启用） |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |

**employee 表（员工）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(255) | 密码 |
| realName | VARCHAR(50) | 真实姓名 |
| phone | VARCHAR(20) | 电话号码 |
| email | VARCHAR(100) | 邮箱 |
| department | VARCHAR(50) | 部门 |
| position | VARCHAR(50) | 职位 |
| status | TINYINT | 状态 |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |

**admin 表（管理员）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(255) | 密码 |
| realName | VARCHAR(50) | 真实姓名 |
| phone | VARCHAR(20) | 电话号码 |
| email | VARCHAR(100) | 邮箱 |
| status | TINYINT | 状态 |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |

#### 2.2.2 包裹业务表

**package 表（正式包裹）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| trackingNumber | VARCHAR(100) | 快递单号，唯一 |
| senderName | VARCHAR(50) | 寄件人姓名 |
| senderPhone | VARCHAR(20) | 寄件人电话 |
| senderAddress | VARCHAR(200) | 寄件人地址 |
| receiverName | VARCHAR(50) | 收件人姓名 |
| receiverPhone | VARCHAR(20) | 收件人电话 |
| receiverAddress | VARCHAR(200) | 收件人地址 |
| packageType | VARCHAR(20) | 包裹类型 |
| weight | DECIMAL(10,2) | 重量 |
| size | VARCHAR(50) | 尺寸 |
| status | VARCHAR(20) | 状态：待入库/已入库/运输中/待取件/已取件/异常 |
| warehouseId | Long | 仓库ID |
| shelfId | Long | 货架ID |
| shelfLayer | Integer | 货架层数（1-4） |
| pickupCode | VARCHAR(50) | 取件码 |
| entryEmployeeId | Long | 入库员工ID |
| entryTime | DATETIME | 入库时间 |
| userId | Long | 关联用户ID |
| pickupDeadline | DATETIME | 取件截止时间 |
| deliveryEmployeeId | Long | 派送员工ID |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |

**package_entry 表（入库记录）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| packageId | Long | 包裹ID |
| employeeId | Long | 操作员工ID |
| warehouseId | Long | 仓库ID |
| shelfId | Long | 货架ID |
| shelfLayer | Integer | 货架层数 |
| entryMethod | VARCHAR(20) | 入库方式 |
| entryTime | DATETIME | 入库时间 |
| remarks | VARCHAR(200) | 备注 |
| createTime | DATETIME | 创建时间 |

**package_outbound 表（出库记录）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| packageId | Long | 包裹ID |
| outboundEmployeeId | Long | 出库员工ID |
| deliveryEmployeeId | Long | 派送员工ID |
| outboundTime | DATETIME | 出库时间 |
| remarks | VARCHAR(200) | 备注 |
| createTime | DATETIME | 创建时间 |

**exception_package 表（异常件）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| packageId | Long | 正式包裹ID |
| trackingNumber | VARCHAR(50) | 快递单号 |
| exceptionType | VARCHAR(50) | 异常类型 |
| exceptionReason | VARCHAR(200) | 异常原因 |
| reportEmployeeId | Long | 报告员工ID |
| reportEmployeeName | VARCHAR(50) | 报告员工姓名 |
| handleEmployeeId | Long | 处理员工ID |
| handleStatus | VARCHAR(20) | 处理状态 |
| handleResult | VARCHAR(200) | 处理结果 |
| source | VARCHAR(20) | 异常来源 |
| userId | Long | 所属用户ID |
| reportTime | DATETIME | 报告时间 |
| handleTime | DATETIME | 处理时间 |
| updateTime | DATETIME | 更新时间 |

#### 2.2.3 基础数据表

**shelf 表（货架）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| shelfCode | VARCHAR(50) | 货架编码，唯一 |
| warehouseId | Long | 仓库ID |
| shelfType | VARCHAR(20) | 货架类型 |
| capacity | Integer | 容量 |
| currentCount | Integer | 当前数量 |
| status | TINYINT | 状态 |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |

**message 表（消息）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 主键，自增 |
| title | VARCHAR(200) | 消息标题 |
| content | TEXT | 消息内容 |
| messageType | VARCHAR(20) | 消息类型 |
| senderType | VARCHAR(20) | 发送者类型 |
| senderId | Long | 发送者ID |
| receiverType | VARCHAR(20) | 接收者类型 |
| receiverId | Long | 接收者ID |
| warehouseId | Long | 仓库ID |
| status | VARCHAR(20) | 状态（未读/已读） |
| sendTime | DATETIME | 发送时间 |
| readTime | DATETIME | 阅读时间 |

---

## 三、功能模块详解

### 3.1 用户角色与权限

系统设计了三种用户角色：

| 角色 | 登录入口 | 主要功能 |
|------|----------|----------|
| 管理员 | 管理员登录 | 运营数据看板、包裹统计、导出数据 |
| 员工 | 员工登录 | 入库管理、出库管理、异常件处理、消息群发 |
| 用户 | 用户登录 | 查看包裹、接收消息、取件 |

### 3.2 认证模块（Auth）

**后端实现：** `AuthController.java` + `AuthService.java`

**功能接口：**

| 接口路径 | 方法 | 功能说明 |
|----------|------|----------|
| `/api/login` | POST | 用户登录验证 |
| `/api/register` | POST | 用户注册 |

**登录流程：**
1. 前端发送登录请求，包含 userType（admin/employee/user）、username、password
2. 后端根据 userType 调用对应的 Repository 查询
3. 验证成功后返回用户信息和 token

**用户类型判断：**
```java
switch (userType) {
    case "admin": authService.loginAdmin(username, password); break;
    case "employee": authService.loginEmployee(username, password); break;
    case "user": authService.loginUser(username, password); break;
}
```

### 3.3 包裹管理模块（Package）

**后端实现：** `PackageController.java` + `PackageService.java`

#### 3.3.1 包裹状态流转

```
待入库 → 已入库 → 运输中 → 待取件 → 已取件
                    ↓
                   异常
```

#### 3.3.2 核心业务流程

**1. 包裹入库（员工操作）**

```
请求: PUT /api/packages/{packageId}/inbound
参数: employeeId, warehouseId, shelfId, shelfLayer

处理流程:
1. 验证包裹是否存在且状态为"待入库"
2. 自动/手动分配货架和层数
3. 生成取件码（格式：{货架ID}-{层数}-{6位随机码}）
4. 更新包裹状态为"已入库"
5. 创建入库记录（package_entry）
6. 发送取件提醒消息给用户
```

**取件码生成逻辑：**
```java
private String generatePickupCode(Long shelfId, Integer shelfLayer) {
    String prefix = shelfId + "-" + shelfLayer + "-";
    // 生成6位随机字符（不含易混淆字符：0,O,1,I,L）
    // 字符集：ABCDEFGHJKLMNPQRSTUVWXYZ23456789
}
```

**货架分配策略：**
- 大件包裹（体积>50000cm³ 或 重量>5kg）：使用货架4（大货架）
- 普通包裹：使用货架1、2、3（普通货架），优先选择使用量最少的货架

**3. 取件超时自动检测（Redis实现）**

> 新增功能：包裹入库后超过指定时间未取件，自动转为异常件

```
实现原理：
1. 包裹入库成功后，将包裹ID写入Redis，设置TTL（过期时间）
2. Redis键过期后自动触发过期事件监听器
3. 监听器调用服务创建异常记录，异常类型为"取件超时"
```

```
Redis键格式：gpot:package:timeout:{包裹ID}
默认过期时间：24小时（可配置）
```

**核心代码逻辑：**

```java
// 1. 入库时写入Redis
String redisKey = "gpot:package:timeout:" + packageId;
redisTemplate.opsForValue().set(redisKey, packageId.toString(),
        pickupTimeoutHours, TimeUnit.HOURS);

// 2. Redis过期事件监听器
@Component
public class RedisKeyExpirationListener extends KeyExpirationEventMessageListener {
    @Override
    public void onMessage(Message message, byte[] pattern) {
        String expiredKey = new String(message.getBody());
        if (expiredKey.startsWith("gpot:package:timeout:")) {
            Long packageId = extractPackageId(expiredKey);
            packageService.handlePickupTimeout(packageId);
        }
    }
}

// 3. 处理超时包裹
public Map<String, Object> handlePickupTimeout(Long packageId) {
    // 检查包裹状态
    // 创建异常记录（异常类型="取件超时"，异常来源="timeout"）
    // 更新包裹状态为"异常"
}
```

**Redis服务端配置要求：**
```bash
# 在redis.conf中添加或执行运行时命令
notify-keyspace-events Ex
```

**配置项（application.properties）：**
```properties
# Redis配置
spring.data.redis.host=localhost
spring.data.redis.port=6379

# 取件超时时间（小时）
package.pickup.timeout-hours=24
```

**业务流程图：**
```
包裹入库 → 写入Redis(TTL=24h) → Redis键过期 → 监听器触发 
                                                        ↓
                                            创建异常记录（取件超时）
                                                        ↓
                                            包裹状态变更为"异常"
```

**取件时清理Redis：**
当用户成功取件后，需要移除Redis中的超时记录，避免重复处理：
```java
// 用户取件时
String redisKey = "gpot:package:timeout:" + packageId;
redisTemplate.delete(redisKey);
```


**2. 包裹出库（员工B操作）**

```
请求: POST /api/packages/{packageId}/outbound
参数: employeeId（出库员工ID）

处理流程:
1. 验证包裹状态为"已入库"
2. 随机分配派送员工
3. 更新包裹状态为"运输中"
4. 创建出库记录（package_outbound）
```

**3. 包裹送达（员工A操作）**

```
请求: POST /api/packages/{packageId}/deliver
参数: employeeId（派送员工ID）

处理流程:
1. 验证包裹状态为"运输中"
2. 验证派送员工身份
3. 更新包裹状态为"待取件"
```

**4. 用户取件（终端机/员工协助）**

```
请求: POST /api/packages/pickup/{trackingNumber}
参数: trackingNumber（快递单号）

处理流程:
1. 根据快递单号查询包裹
2. 验证包裹状态为"已入库"或"待取件"
3. 更新包裹状态为"已取件"
```

#### 3.3.3 异常件管理

**报告异常件：**

```
请求: POST /api/packages/{packageId}/report-exception
参数: exceptionType, exceptionReason, employeeId, source

处理流程:
1. 将正式包裹标记为"异常"状态
2. 创建异常件记录（exception_package）
3. 记录报告人信息和异常原因
```

**异常类型：**
- 包裹破损
- 包裹丢失
- 地址错误
- 收件人信息不符
- 超期未取
- 其他

### 3.4 消息模块（Message）

**后端实现：** `MessageController.java` + `MessageService.java`

**功能接口：**

| 接口路径 | 方法 | 功能 |
|----------|------|------|
| `/api/messages/user/{userId}` | GET | 获取用户消息列表 |
| `/api/messages/user/{userId}/unread` | GET | 获取未读消息 |
| `/api/messages/user/{userId}/unread-count` | GET | 获取未读数量 |
| `/api/messages/{messageId}/read` | PUT | 标记单条为已读 |
| `/api/messages/user/{userId}/read-all` | PUT | 一键已读 |
| `/api/messages/send-to-all` | POST | 群发消息给所有用户 |

**消息类型：**
- 取件提醒
- 系统公告
- 促销信息
- 其他

### 3.5 管理员数据看板

**后端实现：** `/api/admin/*` 接口

**功能接口：**

| 接口路径 | 方法 | 功能 |
|----------|------|------|
| `/api/admin/statistics` | GET | 获取系统统计数据 |
| `/api/admin/daily-entry-statistics` | GET | 获取近N日入库统计 |
| `/api/admin/export-data` | POST | 导出运维数据到文件 |

**统计数据项：**
- 包裹总数、待入库、已入库、运输中、已取件、异常件数量
- 异常件处理统计（待处理、处理中、已处理）
- 用户统计（普通用户、员工、管理员数量）
- 操作统计（入库记录、出库记录数量）
- 近7日入库趋势

**数据导出：**
- 生成格式化的文本报告
- 保存到项目根目录的 GPOT-DATA 文件夹
- 调用 AI 服务生成数据分析

### 3.6 AI分析服务

**后端实现：** `AiService.java`

集成 SiliconFlow API，支持对运维数据进行智能分析。

**配置项（application.properties）：**
```properties
ai.siliconflow.api-key=your-api-key
ai.siliconflow.base-url=https://api.siliconflow.cn/v1
ai.siliconflow.model=Qwen/Qwen2.5-14B-Instruct
```

---

## 四、前端功能模块

### 4.1 路由结构

| 路由路径 | 组件 | 说明 |
|----------|------|------|
| `/` | LoginView | 登录页面 |
| `/debug` | DebugCreatePackageView | Debug调试页 |
| `/terminal` | TerminalQueryView | 终端查询机 |
| `/outbound-machine` | OutboundMachineView | 出库机器 |
| `/main` | MainLayout | 用户布局 |
| `/main/packages` | UserPackagesView | 用户包裹列表 |
| `/main/messages` | UserMessagesView | 用户消息列表 |
| `/welcome` | WelcomeView | 欢迎页 |
| `/admin/dashboard` | AdminDashboardView | 管理员看板 |
| `/exception` | ExceptionView | 异常件页面 |
| `/employee` | EmployeeLayout | 员工布局 |
| `/employee/profile` | EmployeeProfileView | 员工个人信息 |
| `/employee/package/scan` | EmployeePackageView | 扫码入库 |
| `/employee/package/manual` | EmployeePackageView | 手动入库 |
| `/employee/package/exception` | EmployeePackageView | 异常件登记 |
| `/employee/exception` | EmployeeExceptionView | 异常件查询 |
| `/employee/all-packages` | EmployeeAllPackagesView | 全部包裹 |
| `/employee/outbound` | EmployeeOutboundView | 出库操作 |
| `/employee/delivery` | EmployeeDeliveryView | 派送操作 |
| `/employee/send-message` | EmployeeSendMessageView | 消息发送 |

### 4.2 登录页面（LoginView）

- 支持管理员、员工、用户三种登录
- 用户注册功能
- 快速入口：终端查询、出库机器、Debug

### 4.3 管理员看板（AdminDashboardView）

- 实时显示系统运营数据
- 4个标签页：总览、包裹统计、用户统计、操作统计
- SVG折线图展示近7日入库趋势
- 数据导出功能（支持AI分析）
- 自动刷新（30秒间隔）

### 4.4 员工工作台（EmployeeLayout）

- 左侧导航栏
- 功能菜单：
  - 个人信息管理
  - 快递入库（含子菜单：扫码入库、手动入库、异常件登记）
  - 异常件查询
  - 全部包裹
  - 消息发送

### 4.5 终端查询（TerminalQueryView）

- 根据收件人电话号码查询已入库包裹
- 支持取件操作

---

## 五、API接口汇总

### 5.1 认证接口

| 接口 | 方法 | 参数 | 返回 |
|------|------|------|------|
| `/api/login` | POST | userType, username, password | LoginResponse |
| `/api/register` | POST | username, password, realName | LoginResponse |

### 5.2 包裹接口

| 接口 | 方法 | 参数 | 返回 |
|------|------|------|------|
| `/api/packages/user/{userId}` | GET | userId | List<Package> |
| `/api/packages/tracking/{trackingNumber}` | GET | trackingNumber | Package |
| `/api/packages/phone/{phone}` | GET | phone | List<Package> |
| `/api/packages/user/{userId}/all` | GET | userId | Map(formalPackages, exceptionPackages) |
| `/api/packages/user/{userId}/all/paged` | GET | userId, keyword, page, size, type | Page |
| `/api/packages/in-stock` | GET | - | List<Package> |
| `/api/packages/pending-inbound` | GET | - | List<Package> |
| `/api/packages/all` | GET | - | List<Package> |
| `/api/packages/{packageId}/inbound` | PUT | employeeId, warehouseId, shelfId, shelfLayer | Map |
| `/api/packages/{packageId}/outbound` | POST | employeeId | Map |
| `/api/packages/{packageId}/deliver` | POST | employeeId | Map |
| `/api/packages/pickup/{trackingNumber}` | POST | - | Map |
| `/api/packages/{packageId}/report-exception` | POST | exceptionType, exceptionReason, employeeId | Map |
| `/api/packages/transporting/{employeeId}` | GET | employeeId | List<Package> |
| `/api/debug/packages` | POST | (包裹信息) | Package |

### 5.3 消息接口

| 接口 | 方法 | 参数 | 返回 |
|------|------|------|------|
| `/api/messages/user/{userId}` | GET | userId | List<Message> |
| `/api/messages/user/{userId}/unread` | GET | userId | List<Message> |
| `/api/messages/user/{userId}/unread-count` | GET | userId | Long |
| `/api/messages/{messageId}/read` | PUT | messageId | Message |
| `/api/messages/user/{userId}/read-all` | PUT | userId | Map(count) |
| `/api/messages/send-to-all` | POST | title, content, messageType | Map(count) |

### 5.4 管理员接口

| 接口 | 方法 | 参数 | 返回 |
|------|------|------|------|
| `/api/admin/statistics` | GET | - | Map |
| `/api/admin/daily-entry-statistics` | GET | days | List |
| `/api/admin/export-data` | POST | content | Map(fileName, aiAnalysis) |
| `/api/exception-packages` | GET | - | List<ExceptionPackage> |

---

## 六、核心算法与业务逻辑

### 6.1 取件码生成算法

```java
private static final char[] PICKUP_CODE_CHARS = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789".toCharArray();
// 排除易混淆字符：0, O, 1, I, L

private String generatePickupCode(Long shelfId, Integer shelfLayer) {
    String prefix = shelfId + "-" + shelfLayer + "-";
    // 生成6位随机码
    for (int i = 0; i < 20; i++) {
        StringBuilder sb = new StringBuilder(6);
        for (int j = 0; j < 6; j++) {
            sb.append(PICKUP_CODE_CHARS[SECURE_RANDOM.nextInt(PICKUP_CODE_CHARS.length)]);
        }
        String code = prefix + sb.toString();
        // 检查是否已存在
        if (!packageRepository.existsByPickupCode(code)) {
            return code;
        }
    }
    throw new RuntimeException("生成取件码失败");
}
```

### 6.2 货架自动分配算法

```java
private Map<String, Object> allocateShelfAndLayer(String size, BigDecimal weight, Long warehouseId) {
    // 判断是否为大型包裹
    boolean isLargePackage = false;
    
    // 解析尺寸（格式：长x宽x高）
    if (size != null && !size.trim().isEmpty()) {
        String[] dimensions = size.split("x");
        if (dimensions.length == 3) {
            double volume = length * width * height;
            if (volume > 50000 || weight.compareTo(new BigDecimal("5")) > 0) {
                isLargePackage = true;
            }
        }
    }
    
    // 大件用货架4，普通件用货架1-3
    if (isLargePackage) {
        selectedShelfId = 4L;
        selectedLayer = findAvailableLayer(4L, warehouseId, 5);
    } else {
        selectedShelfId = findBestNormalShelf(warehouseId); // 使用量最少的
        selectedLayer = findAvailableLayer(selectedShelfId, warehouseId, 10);
    }
    
    return Map.of("shelfId", shelfId, "shelfLayer", layer);
}
```

---

## 七、配置文件说明

### 7.1 后端配置（application.properties）

```properties
# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/gpot
spring.datasource.username=root
spring.datasource.password=your-password

# JPA配置
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 服务端口
server.port=8080

# AI服务配置
ai.siliconflow.api-key=your-api-key
ai.siliconflow.base-url=https://api.siliconflow.cn/v1
ai.siliconflow.model=Qwen/Qwen2.5-14B-Instruct
```

### 7.2 前端配置（vite.config.js）

```javascript
export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

---

## 八、系统启动指南

### 8.1 后端启动

```bash
cd GPOT
mvn spring-boot:run
```

或使用 IDE 直接运行 `GpotApplication.java`

### 8.2 前端启动

```bash
cd GPOT-F
npm install
npm run dev
```

### 8.3 访问地址

- 前端：http://localhost:5173
- 后端：http://localhost:8080

---

## 九、系统亮点与特色

### 9.1 多角色支持

系统支持管理员、员工、用户三种角色，每种角色拥有独立的功能界面和权限控制。

### 9.2 完整业务流程

覆盖快递驿站的核心业务：入库 → 出库 → 运输 → 送达 → 取件 → 异常处理。

### 9.3 智能货架分配

根据包裹尺寸和重量自动分配货架和层数，优化仓储空间利用。

### 9.4 AI数据分析

集成 AI 大模型，可对运维数据进行智能分析和总结。

### 9.5 可视化看板

为管理员提供多维度的运营数据可视化展示，支持数据导出。

### 9.6 消息通知

内置消息系统，支持取件提醒、群发公告等功能。

---

## 十、开发规范与技术细节

### 10.1 后端技术规范

- 使用 Lombok 简化实体类代码
- 使用 JPA Repository 提供数据访问
- RESTful API 设计风格
- 统一响应格式（ApiResponse）

### 10.2 前端技术规范

- Vue 3 Composition API
- Vue Router 路由管理
- Axios HTTP 客户端
- 组件化开发
- 统一的样式风格（深红色主题）

---

*文档生成时间：2026-03-11*
*项目：GPOT 快递管理系统（毕业设计）*
